public class example {

    // global configuration constant for mining
    int GLOBAL_CONST = 10;

    // build analytics report from repository data
    public void buildReport(int max_items) {

        // track number of processed commits
        int localCount = 0;

        // temporary value used during processing
        String tmp_value = "test";
    }

    /* analyze repository history
       and extract vocabulary tokens */
    public void analyzeRepository() {}
}